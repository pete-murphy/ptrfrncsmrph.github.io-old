import { page as Page } from "@purescript/Page";
export default Page;

export const meta = {
  // This needs to match the type of Page.Props.meta
  // The title will be rendered by Page.mkPage
  title: "First post",
  date: "October 31, 2020",
  description: "An example description",
};

Hello world!

### Interface

<MultiCodeBlock>

```typescript
// interface, abstract type: An interface
//
// In TypeScript, we have to have separate interfaces for the static and
// instance sides of a class

interface ExampleConstructor {
  new (s: string): ExampleInterface;
}
interface ExampleInterface {
  read(): string;
}
```

```purescript
module PureScript where

import Prelude
```

</MultiCodeBlock>
